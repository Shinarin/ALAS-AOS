# 2026-09-25 · 大扫除收官（三模型交叉审计 + 共识收口，本地未 push）

## 一句话状态

大扫除全链路完成：三方审计交叉查验 → 7 批次 10 个 commit（-76,679 行 + 功耗四件套 + CI 从无到有）→ 真机三件套回归通过 → **共识表决达成（kimi/gemini 无保留，deepseek 7 条保留项全闭环，commit `2c7a9e6`）** → 改进方案 `docs/cleanup-consensus-2026-09-25.md` 已交付。**本地累计 13 个 commit，未 push、未发版，等用户指令。**

## 本次做什么了

1. **审计**（grilling 三轮定边界后）：kimi-code / deepseek-flash / gemini-3.8-flash-high 三家独立审计 + 两两互评 + 主代理取证裁决。
2. **批次 A–E 纯删除**（735 文件 +155/-76,679）：`e453629` test 大扫除（54→6，20 tests 绿）；`78568f6` 死 Kotlin/依赖/baselineProfiles/colors；`aec4612` 不可达链（前台模式/亮屏解锁/AIDL 残桩/屏保/AppWatchdog）；`8bbbd6f` :semi-icons 模块；`4151c00` 死字符串 428 组双语。
3. **批次 F 功耗**：`3e8d7ee` wrapper log_lines→log_size（O(1)，双源 cmp）；`f6eb30f` WebView 退后台暂停 + 空闲退避 4s→30s（AppForegroundTracker+poke）+ 失败日志状态沿节流。
4. **批次 G 基建**：`d6f03c7` app.yml CI；`208d6b4` OCR 门禁盲区（azur_lane 模型进镜像+gate 断言 `_al is not None`，**下次 rootfs.yml 跑时才真正闭环**）；`1c76b0b` 文档清扫 + .tmp 回收 7.2GB。

## 关键裁决（下轮审计别再翻案）

- `weights.npz` APK 侧**不是冗余**——保留理由=双源门禁+升级路径保险：G2 起镜像也装 azur_lane 模型，但存量旧 rootfs 没有，APK overlay 是其唯一升级路径；删=旧机升级后 azur_lane numpy OCR 静默回落 PP-OCR 且无报警。（审计裁决当时 build-rootfs.sh 还不装 overlays/models，G2 后措辞以此为准。）
- `app_name` 活（build-logic manifestPlaceholders）；字符串扫描必须含 build-logic。
- `third/IO.java` 活；`BuildProfile.kt` dormant by decision（文件头已注）。
- 豁免区：adb 控制面接口、设置页日志区（另一窗口改造中）、ACCESSIBILITY 悬浮模式。

## 真机回归证据（0.1.6-alpha.11/79 @ AVAY025422002864）

- 装机覆盖成功；`/status` 输出 `log_size`；app.log 节流格式实证。
- 三件套全过：daemon 存活拉起游戏（VD id=46）；event_story 完整跑通自然 finish；挂机 runner_alive + 15min 观察（数据见 devlog）。
- 切页 18 次 0 闪「启动环境」；HOME 35s 回切无损。
- 插曲：Shizuku-m 未运行 → 桥 refused → App 正确显示未运行态；App 内「离线自连」拉起后全链自愈（非本次改动引入）。

## 共识收口（commit `2c7a9e6`，真机已回归）

- deepseek 7 条保留项全闭环：R1/R2 账册勘误；R3 proguard 残留 keep 删除；R4 死指针注释；R6 CI 补守 seeds 对；R7 非 200 记 lastGetFailure。
- **R5** WebView 全局暂停残留：修 `AlasWebViewHolder`（paused 标志位 + register 对齐）。真机实证：切语言重建后 ALAS 控制台正常渲染（修复前永久卡 Loading）；HOME 35s 无损。
- **R8 意外发现**：`.gitignore` 裸 `config/` 致 `maafw.config` 整包从未入库（fresh clone 必炸）——已锚定 `/config/` 并补入库；CI 未首跑先立功。
- F2 WebView 暂停由此获得真机覆盖；F3 冷态后台 30s 退避支路仍未真机覆盖（构建门禁+审查兜底）。

## 未决/下步候选

- **push 授权**：13 个本地 commit（10 代码 + 收口 `2c7a9e6` + 2 账册），等用户一句话。
- 发版与否由用户定（若发版走 `.kimi-code/skills/releasing-new-version` 9 步，版本号建议 v0.1.6）。
- 热更新 CDN `git reset --hard` FAILED（unable to read tree）+ env_fix `template.py WARN restore-failed`：预存现象，待查（非本次改动面）。
- F3 退避 30s 支路未真机覆盖（需冷态+后台场景）；F2 WebView 暂停已由 R5 回归覆盖（切语言重建 + HOME 35s）。
- rootfs.yml 下次运行验证 G2 新断言；若失败第一嫌疑是 chroot 内 cv2/numpy 环境。
- 候选池（用户已知）：src/test 只剩 6 文件可扩充；third/wrappers 死成员（WindowManager.lockNow 等）可再清；发版流程脚本化。

## 环境与命令（不变）

- 构建：`cd app && JAVA_HOME='D:\VSCodeCache\shizku-m\build-env\jdk-17.0.2' GRADLE_USER_HOME='D:\VSCodeCache\maa-alas\.tmp\gradle-home' cmd //c 'gradlew.bat assembleRelease --console=plain'`
- adb 前 `export MSYS_NO_PATHCONV=1`；设备 AVAY025422002864；wrapper 取证 `adb forward tcp:22400 tcp:22400` + curl `/status|/logs?tail=N`。
- Shizuku-m 不在运行时：App 内「离线自连」可拉起（本次开机周期已激活过）。
